{
  "description": "enough info to do a public & static progress dashboard.  no sensitive or private data.  used to generate links and fetch their profile picture",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "their full name"
    },
    "userName": {
      "type": "string",
      "description": "their github handle. for creating a link to their account and their coach bio"
    },
    "modules": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "module": {
            "type": "string",
            "description": "the repo name of the module"
          },
          "role": {
            "type": "string",
            "description": "what role the coach played for this module"
          }
        },
        "description": "the modules this coach taught, and their role in that module"
      }
    }
  }
}
